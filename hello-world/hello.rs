// Simple comment - ignored by the compiler

/* Other way to comment */
fn main() {
  println!("Hello World!");
  println!("I'm a Rustacean!");

  // Formatted print
  let x = 5;
  println!("Is `x` 5 or 50? x = {}", x);
}